/**
 * @deprecated Unused legacy AttributeTable — see features/extraction/components/AttributeTable.tsx
 * for the live implementation. Kept as a stub so any old references compile.
 */
export const AttributeTable = () => null;
export default AttributeTable;
