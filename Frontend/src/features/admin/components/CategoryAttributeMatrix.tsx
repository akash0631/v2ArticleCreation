/**
 * @deprecated Unused component — was superseded by CategoryAttributeMapper.
 * Kept as a stub so old references compile; safe to delete entirely.
 */
export const CategoryAttributeMatrix = () => null;
export default CategoryAttributeMatrix;
