/**
 * @deprecated Unused component — was superseded by HierarchyTreeEditor.
 * Kept as a stub so old references compile; safe to delete entirely.
 */
export const CategoryManager = () => null;
export default CategoryManager;
