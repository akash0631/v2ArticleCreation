/**
 * @deprecated antd theme — antd has been removed.
 * Kept as a no-op so `theme/index.ts` re-exports stay valid until cleanup.
 */
export const antdTheme = {} as Record<string, unknown>;
export default antdTheme;
